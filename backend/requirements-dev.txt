# Vývojové závislosti (testy, lint). Nepatří do produkčního Docker image.
# Instalace: `pip install -r requirements-dev.txt` (vyžaduje již nainstalované requirements.txt).
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
